# 双手机 AI 视觉语音中继测试 v1.2

- 项目：手机视觉 → Gemini 图片理解 → 跨手机输出
- 版本：v1.2-two-phone-voice
- 修改日期：2026-09-10
- 基础版本：v1.1-voice-test

## 应用作用

验证“手机 A 摄像头采集 → Gemini API 分析 → 网络中继 → 手机 B 扬声器朗读”的跨设备闭环。

## 两个页面

- `two-phone-send-v1.2.html`：手机 A。拍照、固定提问“图片中有什么？”，调用既有 `/api/analyze`，把 AI 返回结果发送出去；本机不朗读。
- `two-phone-listen-v1.2.html`：手机 B。只监听手机 A 的 AI 结果并用 Web Speech API 朗读；不拍照、不调用 Gemini。

## 中继方式

测试版使用 `ntfy.sh` 的 HTTP 发布 + SSE(EventSource) 订阅作为临时跨设备中继。配对码用浏览器加密随机数生成，消息发布时使用 `cache=no`，避免服务端缓存。ntfy 公共主题本质上仍是“知道主题名即可访问”，因此这个版本只用于功能测试，后续正式控制小车时应换成自有鉴权中继或设备专用控制通道。

## 使用顺序

1. 手机 A 打开发送页，点击“复制手机B链接”。
2. 把链接发到手机 B 并打开。
3. 手机 B 点击“启动接收并开启语音”，应先听到“接收端已启动”。
4. 手机 A 启动摄像头并点击“拍照并分析发送”。
5. Gemini 返回“图片中有什么”的答案后，手机 A 仅显示并发送；手机 B 自动朗读。
6. 可在手机 A 勾选“每5秒自动拍照 → 分析 → 发给手机B”做连续测试。

## 后续

若此版本验证成功，下一步可以把“手机 B 朗读文本”替换为“手机 B 根据标准动作命令发出不同声音”，再进一步替换为 ESP32/小车电机执行。