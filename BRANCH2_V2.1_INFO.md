# 分支2 v2.1：双手机整页试卷作答中继

- **项目**：全自动写字机 / 双手机视觉-AI-远端输出实验
- **版本**：`v2.1-branch2-full-paper-answer`
- **修改日期**：2026-09-10
- **基础版本**：`v2.0-branch2-question-answer`

## 应用作用

手机 A 主动开启摄像头视频，把整张试卷或整页题目完整放入画面后手动取当前视频帧；后端调用 Gemini 扫描图片中所有可见题号并逐题作答；结果经临时中继发送到手机 B。手机 B 顶部突出显示“题号 + 答案总览”，下方显示逐题解析和识别说明。

## v2.0 问题根因

1. v2.0 的提示词明确写了“识别画面中最主要的一道题”，所以模型只回答一道题，这是程序任务定义错误，不是摄像头漏拍。
2. 原通用 `/api/analyze` 后端把 `maxOutputTokens` 限制在 500，不适合整页试卷的多题答案与解析。
3. v2.0 要求完整转录【题目】，浪费输出额度，也不符合“题号 + 答案优先”的目标。
4. v2.0 预览使用 `object-fit: cover`，屏幕预览可能裁掉边缘，不利于确认整页是否完整入镜。

## v2.1 修改

- 新增 `branch2-question-send-v2.1.html`：整页取景、最高 3200px 截图、0.92 JPEG 质量、调用独立 `/api/solve-paper`。
- 新增 `api/solve-paper.js`：要求扫描所有可见题号，不得只回答一道题；输出 `【答案总览】`、`【详细解析】`、`【识别说明】`；`maxOutputTokens` 提高到 6000。
- 新增 `branch2-answer-receive-v2.1.html`：顶部只显示题号+答案，下方逐题解析。
- 为避免整页长答案超过中继单条消息大小，发送端按 UTF-8 字节拆分成多段，接收端自动重组后再显示。
- 旧 v2.0 页面与旧 API 均保留。
