# 手机自动拍照 + Gemini 识别 + 本机语音播报测试

- 项目：手机自动拍照 + AI 图片分析（具身智能 / 全自动写字机视觉控制前置实验）
- 测试分支：`test-ai-voice-v1.1-2026-09-10`
- 版本：`v1.1-voice-test`
- 修改日期：2026-09-10
- 基础快照：`snapshot-camera-ai-v1.0-2026-09-10`

## 应用作用

验证“摄像头采集 → Gemini 图片理解 → 程序接收 AI 结果 → 手机扬声器产生物理输出”的完整软件链路，为后续把手机扬声器替换为另一台手机或 ESP32 / 电机控制端做准备。

## 本版本新增功能

1. 不再需要用户填写分析问题。
2. 固定向 Gemini 提问：`图片中有什么？`
3. AI 返回结果仍显示在网页上。
4. 用户在每次打开网页后点击一次“开启语音播报”，浏览器会先朗读“语音播报已开启”。
5. 之后每次 Gemini 分析成功，网页自动用 Web Speech API 朗读返回结果。
6. 连续自动分析时，新结果会取消旧朗读并朗读最新结果，避免声音长时间叠加。
7. 保留原有每 5 秒自动拍照、IndexedDB 本地照片、手动分析最新照片、每张照片自动分析、摄像头切换等功能。

## 未改动部分

后端 `api/analyze.js`、`package.json` 与 Gemini API 调用方式继续沿用基础快照；Gemini API Key 仍只在 Vercel 环境变量中保存，不写入前端源码。

## 关键源码

- `index.html`：本版本主要修改文件。
- `api/analyze.js`：继承基础快照，用于调用 Gemini Developer API。
- `package.json`：继承基础快照。

## 下一阶段

若本机语音测试成功，可把“朗读结果”进一步替换为“把标准命令发送给另一台手机”，用第二台手机发声来模拟远程执行器；再之后可把第二台手机替换为 ESP32 / 小车电机控制端。
